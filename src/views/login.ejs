<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            /* we use it because when we use padding it increases the width of the div so we use this so that it not uses the extra width when we use padding */
        }

        .main_div {
            width: 100%;
            height: 100vh;
            position: relative;
            /* border: 10px red solid; */
        }

        .box {
            width: 400px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            padding: 50px;
            background: rgb(0, 0, 0, 0.8);
            border-radius: 10px;
        }

        .box h1 {
            margin: bottom 30px;
            color: #fff;
            text-align: center;
            text-transform: capitalize;
        }

        .box .inputBox {
            position: relative;
        }


        .box .inputBox input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            color: #fff;
            letter-spacing: 1px;
            margin: bottom 30px;
            border: none;
            border-bottom: 1px solid #fff;
            background: transparent;
            outline: none;
        }

        .button {
            
            display: flex;
            justify-content: center;
            background: transparent; 
            border: none; 
            outline: none; 
            color: #fff;
            padding: 8px;
            border-radius: 5px;
            font-size: 16px;
            position: relative;
            top: 10px;
        }
        
    </style>


</head>

<body>
    <!-- <%- include('./partials/nav1.ejs') -%> -->
    <div class="main_div">
        <div class="box">
            <h1>Login Form</h1>
            <form action="/" method="post">
                <div class="inputBox">
                    <input type="email" placeholder="Enter your email" name="email">
                </div>
                <div class="inputBox">
                    <input type="password" placeholder="Password" name="password">
                </div>

                <div class="button">
                    <input type="submit" value="Login" style="height:100%;">
            </form>
            <form action="/register">
                <input type="submit" value="Register">
            </form>
        </div>

    </div>
    </div>
</body>

</html>